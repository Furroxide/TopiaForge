---
title: Development loop
description: Restore, build, test, pack, validate, install, and launch a mod for Robotopia with one command.
---

# Development loop

From a generated mod project for Robotopia, run:

```sh
topiaforge dev
```

Interactive terminals launch Robotopia and tail its attributed logs by default. Redirected output
and CI stop after installation. The complete command surface is:

```text
topiaforge dev [--project path] [--configuration name] [--game-dir path]
               [--launch|--no-launch] [--tail|--no-tail]
```

Use `--project` outside the mod directory, `--configuration` for a bounded MSBuild configuration
name, and `--game-dir` to override Robotopia installation detection. `--launch`/`--tail` opt
redirected automation into the interactive stages; the matching `--no-*` flags make interactive
runs stop earlier.

The V1 development loop performs these stages in order:

1. Seed the release-local SDK source and restore exact package/lock state.
2. Build the mod with the pinned project-local .NET SDK.
3. Run the generated test project.
4. Pack a deterministic `.topiaforgemod` archive.
5. Validate the manifest, managed entry assembly, package layout, and integrity.
6. Install the validated package into the selected Robotopia development profile.
7. Launch Robotopia and tail attributed manager/mod logs.

The command stops on the first failure. It prints a stable `TFDEV` code, the failed stage, a direct
remediation, and a diagnostics URL. It never installs bytes that failed packing or validation.
Validation is metadata-only: the release-carried validator does not load or execute mod code. It
rejects malformed PE files, wrong assembly identities/target frameworks, invalid entry types or
constructors, incompatible SDK references, unsafe exported APIs, and bundled loader-owned SDK files.

## Generated project state

`global.json` pins the supported .NET SDK. `packages.lock.json` pins NuGet resolution and
`topiaforge.sdk.lock.json` records the SDK package set. `topiaforge.dev.props` is generated by
restore and is intentionally untracked; projects never depend on the CLI extraction directory.

Run `topiaforge restore` after adding a module or dependency. Commit `packages.lock.json` and
`topiaforge.sdk.lock.json` so CI and another developer restore the same contract versions. A
multiplayer project also commits generator-owned `topiaforge.multiplayer.lock.json`; refresh it
with `topiaforge mod sync multiplayer` after restore and whenever its multiplayer contract changes.

## Stage failures

Use the first failure as the root cause. A build failure after a restore error is only a consequence,
so the command does not continue. Common fixes are:

- restore: verify the project was created by the same TopiaForge V1 release, then rerun restore;
- build: fix the first compiler or analyzer diagnostic;
- test: run `dotnet test --configuration Release` for full assertion output;
- pack: ensure the entry assembly/type and declared content exist;
- validate: fix every blocking package finding before installation;
- install: choose a writable Robotopia installation/profile and repair its loader if requested; or
- launch: run `topiaforge doctor` and use the reported platform-specific Robotopia path remediation.

The corresponding stable codes are `TFDEV100` restore, `TFDEV105` toolchain, `TFDEV110` build,
`TFDEV120` test, `TFDEV130` pack, `TFDEV140` validate, `TFDEV150` install, `TFDEV160` launch, and
`TFDEV170` tail.

Press Ctrl+C to stop log tailing. The installed mod remains enabled; code changes still require a
Robotopia restart because the in-process runtime cannot replace a loaded assembly.
