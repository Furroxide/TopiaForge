# Maintainer setup plan: isolated RC1 QA

Updated 2026-09-24 for NextActions item 3. The user authorized the remaining Sandbox automation stages and subsequently authorized QA provisioning, the existing build-2409 binary source, themselves as operator, the main display and default audio output. Microphone recording remains off. The separate neutral **release build** setup is still a plan by explicit choice. Account/filesystem preparation does not constitute admitted isolation or candidate acceptance.

## Executed QA preparation

`TopiaForgeQA` is an enabled standard local account. At selection, D: was the healthy fixed NTFS volume with most free space (345,291,829,248 bytes, about 322 GiB). The approved QA root is `D:\TopiaForgeQA`; the account's Windows profile was initialized by the user's confirmed normal first sign-in. No profile relocation or environment spoofing is used.

The root grants QA read/execute access; `game`, `launcher`, `evidence` and `state` permit QA modification, while `source-game` and `tools` are read/execute. The separate `private` directory is restricted to the provisioning administrator, Administrators and SYSTEM. It contains the locally protected bootstrap credential; it must never be printed, copied into Git or sent through chat. Create a separate reviewed record readable by QA when admission is ready; do not expose the bootstrap folder.

At the binary-copy checkpoint, both `source-game` and `game` contained 409 verified binary files (5,428,015,421 bytes each), copied from the explicitly approved existing Robotopia installation. Source and both destination hashes matched. The copy excludes the source installation's runtime injection, personal configuration, saves and logs. This proves equality with that approved source, not independent authenticity of an official archive. The source installation was not modified.

**Build-2478 copies (2026-09-24).** RC1 now pins Robotopia build 2478, so the 2409 copies no longer match. They stay untouched as evidence. Under the user's “Yes, copy 2478”, the build-aware [game copier](../../../tools/copy-sandbox-qa-game.ps1) worked in two steps:

1. It verified the official 2478 install against `.github/robotopia-game-build.json` with `tools/release/verify-robotopia-install.ps1`.
2. It copied exactly the 415 files of the official files manifest into two new protected trees: `source-game-2478` (QA read/execute) and `game-2478` (QA modify), 5,567,020,099 bytes each.

Every file matched its manifest digest in both copies. An independent rehash then found no missing, extra or linked entries. Unlike the 2409 copies, these are bound to the vendor's pinned manifest rather than to an approved local install. The private receipt is `game-copy-2478.json` (SHA-256 `ce72e95cd81118ce37dede363f4786386aa3e4115bbac257ed7ed5689b608804`). No game ran.

The reproducible tools are [account/ACL provisioning](../../../tools/provision-sandbox-qa.ps1) and [bounded game copying](../../../tools/copy-sandbox-qa-game.ps1). The [fresh-provisioning copier](../../../tools/copy-sandbox-qa-provisioning-game.ps1) takes the private record directory and the reviewed copy receipt's digest as parameters, and derives the source tree and counts from that receipt. The original 2409 `game-copy.json` digest is `c6d577dfe8a8fb714a7dcb7dabe9f169e81fde2372d06e319c6a831fdd160118`. Private immutable execution receipts are retained in the ignored review packet. The first account attempt failed on Windows' description-length limit before creating the account or root; its failure receipt was preserved and a corrected attempt completed. No game process had been launched at that account/copy checkpoint; the later failed runtime attempt is recorded below.

The user set the password through the [local secure helper](../../../tools/initialize-sandbox-qa-password.ps1), and its completion receipt was verified. Normal first sign-in is confirmed and Windows reports the QA profile initialized and loaded. The active-session host probe subsequently measured the primary-token identity, native known folders, default render endpoint, main display (3440×1440 at DPI 96) and keyboard/mouse identifiers. It completed with exit 0 and confirmed owned-process exit, without input, capture, microphone or game execution. The later headless attempt recorded an actual Unity path privately but failed on shutdown; a validated clean retry and an attributable private provisioning review remain required. QA preparation is not P0-GAME-01 closure. Do not move or delete any profile or retained evidence to complete these steps.

## Measured host and provisioning-only runtime observation

The successful host probe completed at 20:58:37Z; the one-time task was removed after exit. Raw account, logon and device identifiers remain private. The operator has returned to the normal account; any later observer must run in the active, unlocked standard QA session and revalidate its identity. These metadata observations do not establish physical input, audio playback, visible gameplay or device reservation. See the [measured checkpoint](Evidence.md#qa-host-measurements-and-outbound-isolation-confirmed).

At 21:12:55Z, `TopiaForge-QA-Isolation-20260909` was created and verified as an outbound block for exactly `D:\TopiaForgeQA\game\Robotopia.exe`, covering all profiles, protocols, addresses, ports and interfaces. Existing firewall profiles and services were already enabled; no global firewall settings or source-installation rule changed. Retain the QA block until a reviewed network-policy change.

The [runtime provisioning observer](runtime-provisioning-observer.md) is separately implemented for the authorized preparation step: measure the real Unity persistence path before an isolation record can be reviewed. It requires reviewed exact game/runtime/broker bytes, unused manager state, the existing outbound block and the actual QA identity. It launches a bounded headless game process with the manager inactive, requests no input/capture/audio/microphone, and retains a distinct provisioning result with admission and release qualification false. It does not use an acceptance acknowledgement or guessed path. The separately staged probe binds all 444 QA game/runtime inputs and a 192-file broker; independent hashes, ACLs and wrong-account refusal passed. Its [staging checkpoint](Evidence.md#runtime-provisioning-probe-staged-and-independently-verified) records the exact source and private inputs. The first actual runtime attempt recorded the Unity persistence path privately at 21:38:39Z with `managerInitialized: false`, then **failed** at 21:40:08Z because the game did not exit by its deadline. The retained original process was force-terminated and exit was confirmed; the broker exited 1 without being forced, and the one-time task was removed at 21:40:10Z. No recovery marker remains after confirmed cleanup. This is a failed provisioning attempt, not a successful observation/admission result. Keep the used game tree and all evidence unchanged. The shutdown correction passed source validation and a clean retry was staged; that retry later timed out before broker startup while waiting for the active QA desktop. Successful native execution remains pending. See the [failed execution checkpoint](Evidence.md#runtime-observation-recorded-shutdown-failed). Sandbox/native acceptance remains behind measured admission; candidate execution also requires all release prerequisites.

## Clean provisioning retry

The first failed game remains at `D:\TopiaForgeQA\game`. A separate fresh game is prepared at `D:\TopiaForgeQA\game-provisioning-20260909T215734Z`, using the verified source copy, the corrected v3 loader and its own exact-program outbound block. Its 444 input files, separate 192-file broker and root ACLs passed independent verification; wrong-account execution was refused. The tools/state pair is `runtime-probe-20260909T215900Z`. See the [v3 checkpoint](Evidence.md#deferred-quit-correction-and-clean-retry-staged) for frozen source, tests and private receipts. The dispatched wrapper timed out at 22:13:23Z before broker startup because the QA desktop did not become active; task result 1 and removal were recorded at 22:13:25Z. Retain the failed wrapper output. A further attempt requires reverified inputs, new unused wrapper outputs, the active QA desktop and actual unforced process completion. A subsequent September 11 retry did execute: Update, the Unity quit request and OnApplicationQuit were observed, but the original game again required termination after the deadline. The task was removed. Both game trees are now used evidence. A third fresh copy `D:\TopiaForgeQA\game-provisioning-20260911T180717Z` (409 verified files) carries the v4 diagnostic-mode loader for the staged retry `20260911T183026Z` (tools/state pair `runtime-retry-20260911T183026Z`); it has not run. See the [recorded outcome](Evidence.md#corrected-provisioning-retry-stopped-before-broker-startup).

## Staged development inputs

The authorized development inputs are copied and independently byte-verified:

| Purpose | Actual location |
| --- | --- |
| Frozen dirty source snapshot, 2,695 entries plus 11 loader DLLs | `D:\TopiaForgeQA\source\TopiaForge` |
| Compiled CLI, Windows broker runtime and six development packages | `D:\TopiaForgeQA\tools\sandbox-development-20260909` |
| Exact source and package inventories | `D:\TopiaForgeQA\state\sandbox-development-20260909\source-workspace.json` and `packages.json` |

These are supplementary development inputs based on `4377338`; no final-main candidate was built. The source snapshot excludes Git metadata, personal caches/profiles and unrelated launcher artwork. Loader binaries are allowlisted separately and must match the actual later installed runtime. The independent verifier checked source files and package archives, including manifest and critical-file hashes. Digests and private receipt locations are in [Evidence](Evidence.md#focused-retry-and-verified-qa-development-inputs).

Use the staged compiled CLI without importing the maintainer's SDK or cache. Source/tools are read/execute for QA; its state is writable. The administrator-only bootstrap folder remains inaccessible to QA. Preserve this snapshot and its receipts; later source updates require a new reviewed snapshot and affected builds. The main repository's live launch documents contain subsequent status corrections and remain the current action list.

## Proposed environment and layout

Use a new, separately initialized **standard local Windows account and interactive session on the existing physical Windows host**. This keeps real keyboard, mouse, gamepad, display, audio and microphone available for the required acceptance cases. The account is `TopiaForgeQA` and the user is the authorized operator. The standard account recommendation follows [Microsoft's local-account guidance](https://learn.microsoft.com/en-us/windows/security/identity-protection/access-control/local-accounts); the proposed local account choice serves this QA isolation task. A VM is an alternative requiring a separate plan for graphics and device access.

The authorized account has already been created. The operator has signed in normally and initialized its profile; [Microsoft's account-management instructions](https://support.microsoft.com/en-us/windows/security/identity-signin/manage-user-accounts-in-windows) provide the general Windows procedure. Do not import the maintainer's profile, browser/GitHub credentials, saves, launcher data, authentication tokens, SDK caches or Unity licence files.

The following paths describe the **separate unprovisioned release-build proposal**. They do not replace the approved QA layout under `D:\TopiaForgeQA`. Do not provision these release inputs until the user changes the explicit plan-only decision.

| Purpose | Proposed physical location |
| --- | --- |
| Neutral release root | `C:\TopiaForgeBuild` |
| Clean final-main checkout | `C:\TopiaForgeBuild\source` |
| Transaction state/worktrees | `C:\TopiaForgeBuild\state`; generated worktrees stay under its version directory |
| Fresh official SDK and cache | `C:\TopiaForgeBuild\flutter-3.44.6` including Dart 3.12.2; fresh `C:\TopiaForgeBuild\pub-cache` |
| Verified source game | `C:\TopiaForgeBuild\source-game`; proposed future release copy only. The separately authorized development source has already been copied under `D:\TopiaForgeQA` |
| Separate clean QA game / launcher | `C:\TopiaForgeBuild\qa\game` / `C:\TopiaForgeBuild\qa\launcher` |
| Exact acceptance outputRoot | `C:\TopiaForgeBuild\state\0.1.0-rc.1\evidence\windows\robotopia` |
| Private provisioning record | `C:\TopiaForgeBuild\private\isolation.json`, outside every cleared build/evidence output |

Use ordinary local directories throughout, including existing ancestors: no junctions, symlinks, reparse points, `subst`, UNC or profile-linked SDK aliases. QA game, launcher and evidence roots must be separate and must not overlap the source installation or normal-player profile. Unity persistent data belongs under the **measured QA LocalAppDataLow**; its actual application subdirectory must be observed, not assigned from this table.

## Provisioning sequence after approval

1. **Confirm owners and install source.** Name the QA operator, provisioning administrator and reviewer. Identify the authorized build-2409 source, separate licensed clean QA installation method, intended host and available hardware. No personal save/token/cache import; any required application sign-in happens legitimately in the QA context. An isolated OS account is required even with a separate BepInEx profile.
2. **Create the account and narrowly scoped access.** Initialize the account through a real Windows sign-in. Provision only the approved physical roots. Limit ACLs to designated administrator/QA identities and required operating-system principals: source game and tools need appropriate read/execute; QA game, launcher, evidence and fresh build caches need the required QA write access. Restrict the private record to its designated author/reviewer and QA read access. Review inheritance; never grant Everyone access or modify the normal player's profile ACLs. Keep GitHub credentials and signing material in the administrator context.
3. **Prepare neutral compilation inputs separately.** Use a real Git checkout with metadata and hydrated LFS, fresh pinned official Flutter/Dart, and a fresh dedicated PUB_CACHE restored through checked-in lockfiles. Do not reuse personal `.dart_tool`, generated SDK caches or outputs. Inspect the preferred `.fvm/flutter_sdk` resolution; PATH cannot override a preferred personal FVM SDK. Apply PATH/PUB_CACHE only in the designated release process; do not spoof HOME, USERPROFILE or APPDATA. Verify the remaining pinned toolchain in [AdminRelease](../../../docs/AdminRelease.md#admin-windows-machine). Unity 6000.0.23f1 was already installed at `C:\Program Files\Unity\Hub\Editor\6000.0.23f1`; legitimate licence admission in the QA session still needs verification and any necessary authorization.
4. **Measure and review isolation before candidate execution.** In the initialized QA session, obtain identity through the primary process token and native Windows known-folder APIs used by [the implementation](../../../packages/launcher_data/lib/src/launch_process_isolation_windows.dart). The host probe has measured native identity/known folders and device identifiers. Observe the real game's `Application.persistentDataPath` through the authorized [inert headless provisioning observer](runtime-provisioning-observer.md) before freezing the record. Do not guess the vendor/game folder or use environment overrides as evidence. Review source/QA separation, physical paths, ACLs and absence of normal-user data access, then record actual reviewer authorization and proof privately.
5. **Admit only an eligible frozen candidate.** Complete all four non-game prerequisite decisions (`P0-IP-01`, `P0-OSS-01`, `P0-PRIV-01`, `P0-CRED-01`) and freeze the actual two-parent final-main merge SHA with a clean checkout equal to origin/main. Credential-incident closure was explicitly deferred by the user, so candidate build remains blocked. Authenticated preflight runs in the release administrator context with explicit source GameDirectory, StateRoot and approved AcceptanceIsolationRecord. The frozen-state build and game acceptance run in the actual QA session using the same controlled paths; qualification/publication return to the administrator context. Never copy the administrator credential store to QA.

## Private evidence contract and later acceptance

The [private provisioning parser](../../../packages/launcher_data/lib/src/acceptance_isolation_context.dart) requires exactly: `schemaVersion` (integer 1), `kind` (`windows-user` for this proposal), `sourceGameRoot`, `gameRoot`, `launcherRoot`, `outputRoot`, `persistentDataRoot`, `userSid`, `userProfile`, `localAppDataLow`, `normalUserSid`, `normalUserProfile`, and `reviewerEvidence`. All identity/path values must describe measured reality. Here `reviewerEvidence` is a nonempty **string** (at most 1024 characters), referencing actual private provisioning review. No placeholder attestation JSON is supplied by this plan.

The later [detached candidate acceptance record](../../../apps/topiaforge_cli/lib/src/release_candidate_acceptance.dart) instead uses a nonempty `reviewerEvidence` **array** of objects containing exactly `evidenceId`, `role`, `reference`, `sha256`. It must cover each GAME evidence ID and both `robotopia-owner` and `runtime-mod-qa` roles, with valid `review:` references and actual evidence hashes. This is separate from provisioning approval.

Schedule the human operator only after prerequisites and exact candidate bytes exist. [LiveGameAcceptance](../../../docs/LiveGameAcceptance.md) requires real input/audio/visual observations, 15 SDK cases, 10 game lifecycle cycles and the 36 gamemode cases; 16 pinned-Unity authoring cycles are separately required. The earlier 14/14 Editor tests do not satisfy these candidate checks. Preserve private raw logs, measured identity and correlated runtime acknowledgement on the QA host; public records contain only reviewed summaries/digests. Do not delete a layout while exit of its owned process is unconfirmed.

## Remaining initialization and review

- Preserve the failed runtime game/evidence tree, the successful host probe and its first timed-out attempt. The shutdown correction reached its intended native callbacks on September 11 but did not produce unforced exit. Preserve that failure and the earlier desktop-wait failure. Diagnose and verify improved shutdown logging before another active-session attempt; preserve both used game trees. Identify the attributable provisioning reviewer.
- After successful unforced completion, review the actual Unity path with the measured native identity/known folders, device identifiers, physical roots and ACLs. The first raw path record remains useful failure evidence; it does not pass provisioning. Device reservation and reviewer approval remain separate from metadata collection.
- Complete authorized Sandbox development acceptance only after measured admission. Candidate build/acceptance remains gated by its separate prerequisites; further licence activations and device installations are not implied.

Item 3 is partly executed: account, roots, verified binary copies, independently verified development inputs, native host/device measurements and the QA-only outbound block exist. The first headless run recorded a private Unity path but failed on shutdown; a validated clean retry, reviewed admission, admitted environment execution and P0-GAME-01 remain pending. See [NextActions](NextActions.md), [session decisions](Decisions.md), [neutral build requirements](../../../docs/AdminRelease.md#build-from-a-neutral-root) and the [release checklist](../../../docs/ReleaseChecklist.md).

Preparation decisions and pending authorization are recorded in [Decisions](Decisions.md); see [NextActions](NextActions.md) before execution. Questions require an explicit reply, with no timeout-based default.

The 2026-09-11 retry ran the game and reached Unity OnApplicationQuit, but again exceeded the 90-second deadline. Original-game exit was confirmed after forced cleanup; the task was removed. The diagnostic launch mode that retains the Unity log and read-only runtime snapshots is verified on a fixture player, and retry `20260911T183026Z` is staged and refusal-checked without dispatch. No attempt is queued; one bounded operator session, confirmed by the user, comes next. See the [observer runbook](runtime-provisioning-observer.md) for the retained outcomes and the prepared retry.
